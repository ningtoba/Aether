/**
 * @aether/security — public barrel.
 *
 * RBAC (role-based access control) with hierarchical roles and glob
 * resource matching. API-key authentication for the HTTP server lives in
 * `@aether/backend`.
 */
export * from './rbac.js';
